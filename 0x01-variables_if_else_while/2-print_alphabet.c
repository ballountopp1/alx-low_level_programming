#include <stdio.h>
#include <stdlib.h>


/**
 * main - returs alpha
 * Return: Always 0 (Success)
 */

int main(void)
{
	char c;
	int ch = 'a';
	while (ch <= 'z');
	{
		putchar(ch);
		ch++;
	}
		putchar('\n');
	return (0);
}
